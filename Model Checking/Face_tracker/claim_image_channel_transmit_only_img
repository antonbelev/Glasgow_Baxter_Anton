never  {    /* <>(nemptyImgTopic && !imgTopicPoll) */
T0_init:
	do
	:: atomic { ((nemptyImgTopic && !imgTopicPoll)) -> assert(!((nemptyImgTopic && !imgTopicPoll))) }
	:: (1) -> goto T0_init
	od;
accept_all:
	skip
}
