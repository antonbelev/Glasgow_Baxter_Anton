never  {    /* ([]<> !np_) -> (<>(p && !q)) */
T0_init:
	do
	:: atomic { ((p && !q)) -> assert(!((p && !q))) }
	:: ((np_)) -> goto accept_S5
	:: (1) -> goto T0_init
	od;
accept_S5:
	do
	:: ((np_)) -> goto accept_S5
	od;
accept_all:
	skip
}
